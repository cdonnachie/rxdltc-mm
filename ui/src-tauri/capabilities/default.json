{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs, dialogs for picking files/folders. All bot/KDF access goes through Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
